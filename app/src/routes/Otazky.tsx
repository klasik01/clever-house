// DEPRECATED — V3.1 merged /napady + /otazky into /zaznamy.
// File kept as empty stub because filesystem perms forbid deletion in this environment.
// Safe to git rm on your side.
export default function DeprecatedRoute() {
  return null;
}
