/**
 * V25 cleanup — orphan test file. Real source removed; this stub just keeps
 * vitest happy until `git rm` is run.
 */
import { describe, it, expect } from "vitest";

describe.skip("orphan — V25 cleanup pending git rm", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
