/**
 * V25 cleanup — tento soubor je mrtvý kód (orphan po V20-S40 refactoru).
 * Zůstává jen jako prázdný stub, dokud `git rm` nezbaví repo úplně.
 * Reference: `.design/canonical-status-flow/DESIGN_REVIEW.md` (V25 cleanup
 * appendix). Pro vyčistění spusť:
 *   git rm app/src/lib/{enums,eventsFilter,eventsFilter.test,prehled,prehled.test}.ts \
 *          app/src/routes/{Harmonogram,Lokace,Prehled}.tsx
 */
export {};
